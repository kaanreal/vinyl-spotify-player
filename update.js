
setInterval(checkisPlaying, 1000); // Updates playback status every second
setInterval(isplaying, 1000);    // Checks playback status every second
setInterval(updateCover, 1000);       // Updates the cover every second
setInterval(switch_playPause, 1000);  // Updates the play/pause button every second


